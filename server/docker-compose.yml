services:
  db:
    image: postgres:16-alpine
    container_name: brevly-db
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: brevly
    ports:
      - '5433:5432'
    volumes:
      - brevly-pg:/var/lib/postgresql/data

volumes:
  brevly-pg:
